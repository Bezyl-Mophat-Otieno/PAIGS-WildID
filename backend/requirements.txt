# Web framework
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# Data validation
pydantic>=2.7

# Database
sqlalchemy>=2.0
psycopg2-binary>=2.9   # PostgreSQL driver (SQLite used by default for prototype/tests, no driver needed)

# Bioinformatics (in use from Stage 1: format validity checking)
biopython>=1.83
numpy>=1.26
pandas>=2.2

# Reporting (used from Stage 11: PDF report generation)
reportlab>=4.2

# Testing
pytest>=8.2
httpx>=0.27   # required by FastAPI's TestClient
pypdf>=6.0    # test-only: extracts real text back out of Stage 11's generated
              # PDFs so tests can assert on report *content*, not just that
              # bytes came out the other end
