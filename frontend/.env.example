# Leave unset for local dev -- vite.config.ts proxies API paths to
# http://localhost:8000 (the backend's docker-compose dev port). Set this
# only for a build that talks to a deployed backend directly.
# VITE_API_BASE_URL=https://api.wildid.example.com
